# Frontend Standard

## Required

- Use accessible components and semantic HTML.
- Keep state ownership explicit.
- Provide loading, empty and error states for async UI.
- Prefer design system components over one-off UI.
- Test critical user flows.

## Avoid

- Client-only authorization.
- Generic UI without domain context.
- Layouts that hide validation or error feedback.
