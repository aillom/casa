# Harness History

C.A.S.A records local harness actions here when commands generate plans, recipes or provider configuration.

History entries are JSON Lines and should contain command intent, generated files, selected packs, selected recipes and validation notes.

Do not store secrets, raw provider responses or sensitive production data in history.
