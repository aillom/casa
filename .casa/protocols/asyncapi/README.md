# AsyncAPI Protocol

## Role

AsyncAPI defines event, queue and message contracts.

## C.A.S.A usage

- Use for durable event contracts and cross-service queues.
- Document message schema, publisher, consumer and compatibility.
- Treat breaking message changes like API breaking changes.
