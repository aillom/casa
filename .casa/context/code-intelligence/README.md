# Code Intelligence

This folder is reserved for generated indexes such as symbols, call graphs, dependency graphs and API surface maps.

## Current state

No code intelligence index is required for the v0.1 repository because the project is mostly Markdown and local Node.js scripts.

## Future files

- `symbols.index.json`
- `call-graph.json`
- `dependency-graph.json`
- `api-surface.json`
