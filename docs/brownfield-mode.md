# Brownfield Mode

Brownfield Mode is used for existing and legacy systems.

It installs C.A.S.A as an overlay without forcing immediate rewrites.

The first goal is not to refactor. The first goal is to understand, map, protect and then modernize incrementally.
